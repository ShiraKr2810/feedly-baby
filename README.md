# Feedly Baby

אפליקציית React + TypeScript למעקב הנקה, שאיבות, בקבוקים, טיטולים וסיכומי לילה.

## הרצה

```bash
npm install
npm run dev
```

האפליקציה שומרת נתונים ב-LocalStorage בלבד, ללא שרת וללא Firebase.
